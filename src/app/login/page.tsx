import { LoginComponent } from '@/components/LoginComponent';

export default function LoginPage() {
  return (
    <div className="min-h-[600px] flex items-center justify-center px-4">
      <LoginComponent variant="page" />
    </div>
  );
}

